#!/bin/zsh

find $GRABBOX_PATH -mtime +$GRABBOX_CLEANUP_LIFETIME -type f -name "*.png" -delete

terminal-notifier \
  -title "GrabBox" \
  -message "Cleanup done" \
  -sound Pop \
  -contentImage $HOME/Applications/GrabBox.app/Contents/Resources/GrabBox.icns \
  -open file://$GRABBOX_PATH
