#!/bin/zsh

brewinstall zsh-completions
brewinstall terminal-notifier

#brewcaskinstall "iTerm" "iterm2" $HOME/Applications

#source $DOTFILES/preferences/Terminal.prefs
#source $DOTFILES/preferences/iTerm2.prefs

# safer than rm
npmig trash
