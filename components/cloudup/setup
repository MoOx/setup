#!/bin/zsh

brewcaskinstall "Cloudup" $HOME/Applications
defaults write com.cloudup.Cloudup moveToApplicationsFolderAlertSuppress 1

# openifnot "Cloudup" "$HOME/Applications/Cloudup.app"

defaults write com.cloudup.Cloudup autoupload 1
